import { Direction } from "../enums/direction";

export const RotateLeft: Record<string, Direction> = {
    N: Direction.West,
    W: Direction.South,
    S: Direction.East,
    E: Direction.North,
};

import { Direction } from "../enums/direction";

export const RotateRight: Record<string, Direction> = {
    N: Direction.East,
    E: Direction.South,
    S: Direction.West,
    W: Direction.North,
};
